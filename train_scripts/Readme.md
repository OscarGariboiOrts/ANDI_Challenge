Scripts for training the Keras models both for Task1 (diffusion exponent regression) and Task2 (classification).

Training datasets have to be generated previously using the andi_datasets Python library.
